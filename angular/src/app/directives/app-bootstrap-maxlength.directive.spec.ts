import { AppBootstrapMaxlengthDirective } from './app-bootstrap-maxlength.directive';

describe('AppBootstrapMaxlengthDirective', () => {
  it('should create an instance', () => {
    const directive = new AppBootstrapMaxlengthDirective();
    expect(directive).toBeTruthy();
  });
});
